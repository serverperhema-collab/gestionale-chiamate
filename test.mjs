import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const targetDate = new Date('2026-07-23'); targetDate.setHours(0, 0, 0, 0); try { const agenda = await prisma.zoneAgenda.upsert({ where: { date: targetDate }, update: { caps: ['00118', '00119'] }, create: { date: targetDate, caps: ['00118', '00119'] } }); console.log('Success:', agenda); } catch (e) { console.error('Error:', e); } finally { await prisma.$disconnect(); } } main();
