import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const appts = await prisma.appointment.findMany({ where: { zoneAgendaId: { not: null } }, include: { zoneAgenda: true } }); let count = 0; for (const appt of appts) { const apptDate = new Date(appt.date); const agendaDate = new Date(appt.zoneAgenda.date); if (apptDate.getFullYear() !== agendaDate.getFullYear() || apptDate.getMonth() !== agendaDate.getMonth() || apptDate.getDate() !== agendaDate.getDate()) { apptDate.setFullYear(agendaDate.getFullYear(), agendaDate.getMonth(), agendaDate.getDate()); await prisma.appointment.update({ where: { id: appt.id }, data: { date: apptDate } }); count++; console.log('Fixed appt', appt.id, 'to date', apptDate); } } console.log('Fixed', count, 'appointments.'); } main().catch(console.error).finally(() => prisma.$disconnect());
